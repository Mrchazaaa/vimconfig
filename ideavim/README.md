# Setup

From the repo root, run:

```bash
./install.sh --ideavim
```

Or run the IdeaVim-only installer directly:

```bash
./ideavim/install.sh
```

This writes `~/.ideavimrc` as a shim that sources `ideavim/.ideavimrc`.
